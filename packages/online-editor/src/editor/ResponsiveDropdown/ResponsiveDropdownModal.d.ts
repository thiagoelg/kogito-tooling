import { ReactNode } from "react";
export declare type ResponsiveDropdownModalProps = {
  isOpen?: boolean;
  className?: string;
  title: string;
  onClose?: () => void;
  children?: ReactNode;
};
export declare function ResponsiveDropdownModal({
  isOpen,
  onClose,
  title,
  children,
}: ResponsiveDropdownModalProps): JSX.Element;
//# sourceMappingURL=ResponsiveDropdownModal.d.ts.map
